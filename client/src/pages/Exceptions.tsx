import { EmptyPanel, PageTitle, StatusBadge } from "@/components/arcway/primitives";
import { trpc } from "@/lib/trpc";
import { AlertTriangle, ArrowRight } from "lucide-react";
import { Link } from "wouter";

export default function Exceptions() { const query = trpc.trades.list.useQuery(undefined, { retry: false }); const trades = query.data ?? []; return <div className="mx-auto max-w-[1540px] animate-enter"><PageTitle eyebrow="Exception inbox" title="Resolve the drift between sources." description="ARCWAY surfaces conflicts where documents no longer describe the same commercial, physical, or financial trade." />{trades.length === 0 ? <EmptyPanel title="No exceptions to triage" description="When evidence is uploaded, ARCWAY compares key fields across documents and records any inconsistencies here." /> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{trades.map(trade => <Link key={trade.id} href={`/trades/${trade.id}`} className="panel block p-5 transition-colors hover:bg-white/[0.04]"><div className="flex items-start justify-between"><div className="grid h-9 w-9 place-items-center rounded-lg bg-amber-400/[0.1] text-amber-300"><AlertTriangle className="h-4 w-4" /></div><StatusBadge status={trade.assuranceState} kind="assurance" /></div><p className="mt-5 font-mono text-xs text-blue-300">{trade.reference}</p><p className="mt-1 text-sm font-medium text-slate-200">{trade.sellerName} → {trade.buyerName}</p><p className="mt-2 text-xs leading-5 text-slate-500">Open the Trade Twin to review evidence consistency, assign a reviewer status, and record an auditable resolution.</p><span className="mt-4 inline-flex items-center text-xs font-medium text-blue-300">Open workspace<ArrowRight className="ml-1.5 h-3.5 w-3.5" /></span></Link>)}</div>}</div>; }

